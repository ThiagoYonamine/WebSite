package com.ufscar

class AdaptativoController {

    def index() { }

    //Adaptação
    def like() {
        /*def u = session.getAttribute("usr")
        String atributo = params.descricao
        if(atributo == "parques"){
            return u.parques + 1
        }
        if(atributo == "museus"){
            return  u.museus + 1
        }*/
    return  10

    }
    //Fim adaptação
}
